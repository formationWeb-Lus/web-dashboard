<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Connexion</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <h1>Connexion</h1>
    <p>Enter your phone number and password to log in.</p>

    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>

    <form method="POST" action="/login">
      <!-- Champ téléphone -->
      <input type="text" name="phone" placeholder="Téléphone" required minlength="8" />

      <!-- Champ mot de passe -->
      <input type="password" name="password" placeholder="password" required minlength="6" />

      <button type="submit">connect</button>
    </form>

    <a href="/register" class="link">Create a compt if you don't have</a>
  </div>
</body>
</html>

