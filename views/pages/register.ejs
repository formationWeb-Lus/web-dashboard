<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Créer un compte</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <h1>Créer un compte</h1>
    <p>Remplissez les informations ci-dessous pour vous inscrire.</p>

    <% if (error) { %>
      <div class="error"><%= error %></div>
<% } %>

<form method="POST" action="/register">
  <input type="text" name="firstname" placeholder="Prénom" required />
  <input type="text" name="lastname" placeholder="Nom" required />
  <input type="tel" name="phone" placeholder="Téléphone" required minlength="8" />

  <label>Choisissez un plan :</label>
  <select name="plan" required>
    <option value="">-- Sélectionnez --</option>
    <option value="mensuel">Mensuel - 10$/véhicule</option>
    <option value="annuel">Annuel - 100$/véhicule</option>
  </select>

  <label>Nombre de véhicules :</label>
  <input type="number" name="vehicleCount" min="1" max="300" value="1" required />

  <button type="submit">S’inscrire</button>
</form>

<a href="/login" class="link">J’ai déjà un compte</a>
