<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create an Account</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <h1>Create an Account</h1>
    <p>Fill in the information below to sign up.</p>

    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>

    <form method="POST" action="/register">
      <input type="text" name="firstName" placeholder="First Name" required />
      <input type="text" name="lastName" placeholder="Last Name" required />
      <input type="tel" name="phone" placeholder="Phone Number" required minlength="8" />
      <input type="password" name="password" placeholder="Password" required />

      <label>Choose a plan:</label>
      <select name="selectedPlan" required>
        <option value="">-- Select --</option>
        <option value="monthly">Monthly - $5/vehicle</option>
        <option value="yearly">Yearly - $100/vehicle</option>
      </select>

      <label>Number of vehicles:</label>
      <input type="number" name="vehicleCount" min="1" max="300" value="1" required />

      <button type="submit">Sign Up</button>
    </form>

    <p>Already have an account? <a href="/login" class="link">Log in here</a></p>
  </div>
</body>
</html>
