<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Dashboard GPS</title>
  <style>
    body { font-family: Arial; margin: 20px; }
    .nav { display: flex; gap: 15px; list-style: none; padding: 0; background: #eee; margin-bottom: 20px; }
    .nav li a { text-decoration: none; padding: 8px 12px; display: block; }
    .nav li a.active, .nav li a:hover { background: #ccc; }
    table { border-collapse: collapse; width: 100%; }
    th, td { border: 1px solid #ddd; padding: 8px; text-align: center; }
  </style>
</head>
<body>
